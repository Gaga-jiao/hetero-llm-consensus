# 我让 3 家不同厂商的 AI 一起投票，结果它们一起错

> 一篇关于"多智能体共识"的实测报告。全部代码、原始数据、复现脚本见本仓库。

## 起因

现在有个很流行的说法：让多个 AI 模型互相讨论、投票，就能得到比单个模型更可靠的结果。工程圈里各种 "multi-agent debate / council of models" 的工具也越出越多。

我原本也准备做一套"多智能体裁决器"，用在自己日常的 Agent 工作流里 —— 让几个不同厂商的模型对同一个结论投票，不一致时再由第三方裁定。

但动手前先做了一个最小验证：**这些模型的"自信"到底能不能信？**

## 实验

- **题库**：SimpleQA 随机 30 题（OpenAI 开源的事实性基准，专门测"模型自信地答错"）。都是长尾事实题，比如"2010 年 IEEE Frank Rosenblatt 奖颁给了谁"。
- **模型**：3 家不同厂商 —— DeepSeek、智谱 GLM、Groq compound。
- **做法**：每题每个模型独立采样多次（DeepSeek 10 次、智谱 5 次、Groq 2 次），要求模型给出答案 + 一行自报置信度。

结果先说难度：**成绩最好的一家，30 题也只答对 23%**。这批题确实把它们打下来了 —— 这正好是我们要的：错误样本足够多，才能测出"错误是怎么分布的"。

## 发现一：模型的自报置信度，几乎没有信息量

| 模型 | 平均自报置信度 | 实际准确率 |
|---|---|---|
| 智谱 GLM | **0.860** | 13% |
| DeepSeek | 0.738 | 23% |
| Groq compound | 0.585 | 22% |

最自信的模型准确率最低。整体校准误差 **ECE = 0.537**（完美校准是 0）：模型说"我 0.96 确定"时，实际只有 31.8% 正确。

我试了标准的温度缩放校准，只能把 ECE 从 0.537 降到 0.388 —— **救不回来**。

## 发现二：换厂商，并没有换来"独立的错误"

这是最出乎我意料的一条。

直觉上，不同厂商的模型权重不同、训练数据不同，错误应该比较独立，投票才有意义。但实测：

- 不同厂商两两之间，正确性相关性 **phi = +0.58**
- 同一个模型 10 次独立采样之间，相关性 **phi = +0.74**

也就是说：**换厂商确实降低了一点相关性（0.74 → 0.58），但仍然高度相关。** 它们会在同一批题上一起错 —— 因为这些长尾事实的盲区是"整个行业"共有的，不是某一家模型的怪癖。

换个说法：让 3 家模型投票，约等于让 1 个模型回答 3 次，只是好一点点。

## 发现三：投票没赢过最好的单模型，置信度加权还更差

| 策略 | 准确率 |
|---|---|
| 最好单模型 | 23% |
| 等权多数投票 | 23% |
| 自报置信度加权 | **13%** |
| 温度校准后加权 | **13%** |
| 自一致性加权 | 23% |

两个结论：

1. **多数投票相对单模型最好成绩是零增益** —— 花 3 倍成本，换来一样的准确率。
2. **置信度加权是有害的**：23% 掉到 13%。原因就是发现一：置信度跟正确率是负相关的，加权等于**优先听最自信的那个，而最自信的往往是错得最离谱的**。

顺带一提：唯一有点用的信号是**自一致性**（同一模型多次采样的一致率）。一致率 0.20 的题只有 6.2% 正确，一致率 1.00 的题有 26.7% 正确。但注意 26.7% 也不高 —— **"一致地错"是常态**。

## 所以，多智能体投票还有意义吗？

有，但要换个用法。

**不要期望**：多找几个模型投票 → 答案更可靠。这次实测不支持这个预期。

**可以这样用**：

1. **把自一致性当风险信号，而不是正确性保证**：一个模型对某题三次采样答案都不一样，说明它对这题没把握 —— 这时候才值得升级处理（换更强的模型、去检索、或直接问人）。
2. **要"独立"，得换信息源而不是换模型**：真正让错误不相关的是**不同的证据**（一个查文档、一个跑代码、一个查数据库），而不是不同的权重。
3. **置信度不能直接采信，必须校准**：而且在我们这批难任务上，标准校准方法也救不回来。
4. **省钱**：既然投票零增益，那就把资源花在"让单个模型拿到更好的上下文/工具"上，比多开几个模型划算。

## 关于这份实验的诚实说明

- **只有 3 家模型**。原计划 8 家，但免费渠道额度当天就耗尽了（OpenRouter 免费层、Groq 免费渠道），另外几个 key 也有欠费/失效的问题。3 家的相关性统计置信区间偏宽，结论方向可信、具体数值别当精确值。
- **30 道题**，判分用字符串匹配（没用官方 LLM judge）。
- 原始数据全部公开在 `data/raw.jsonl`，欢迎用更严格的方法重判、或扩大规模重跑。

## 复现

```bash
pip install -r requirements.txt
python src/analyze.py          # 出表出图
python src/analyze.py --selftest   # 统计函数自检
```

题库来自 OpenAI 的 SimpleQA（`openai/simple-evals`），采样种子固定为 `20260911`，抽出的 30 道题在 `data/questions.json` 里。

---

*本实验属于一个更大的问题：多智能体系统（MAS）的可靠性到底能不能靠"冗余"买来。答案看起来是：能买一点，但比想象中贵得多，而且买法跟直觉相反。*
